#' @param travel_cost_col A `string` with the name of the column with travel
#'   cost between origns and destinations. It can be either either travel time,
#'   distance or monetary costs. Defaults to `travel_ime`.
